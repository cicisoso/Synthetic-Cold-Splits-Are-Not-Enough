\section{Results}

\subsection{Synthetic cold splits are already regime-dependent}

We begin with the \texttt{BindingDB\_Kd} synthetic reference panel. Table~\ref{tab:main-results} shows the main pattern: even within a single dataset, model rankings are not stable across split definitions. On \texttt{unseen\_drug}, \texttt{HyperPCM} is strongest by mean \AUPRC{}, reaching $0.5694 \pm 0.0473$ versus $0.5509 \pm 0.0900$ for \texttt{base} and $0.5004 \pm 0.0341$ for \texttt{DTI-LM}. On \texttt{blind\_start}, \texttt{HyperPCM} has the highest mean \AUPRC{} at $0.3828 \pm 0.1024$, but the margin over \texttt{base} is much smaller, the variance is the largest in the main table, and paired bootstrap crosses zero; we therefore treat this row as having no reliable winner. On \texttt{unseen\_target}, the pattern changes again: \texttt{base} is strongest by mean \AUPRC{} at $0.5224 \pm 0.0602$, while \texttt{DTI-LM} is slightly lower on \AUPRC{} but higher on mean \AUROC{}.

\input{tables/table_main_results}

These results matter because they show that ``synthetic cold-start DTI'' is already not a single evaluation condition. Even after replacing internal probes with external literature baselines, different synthetic regimes still reward different model families, so it is unsafe to generalize from one split to another without explicit evidence. The review-driven similarity diagnosis sharpens this point further: on synthetic \texttt{unseen\_drug}, the held-out compounds still have median nearest-train Tanimoto $0.7732$ and Murcko scaffold match rate $0.6204$. When we replace entity holdout with a stricter scaffold split, the earlier \texttt{HyperPCM} edge is no longer supported. Over three seeds on \texttt{BindingDB\_Kd / scaffold\_drug}, \texttt{base} reaches $0.6069 \pm 0.0161 / 0.8351 \pm 0.0037$, while \texttt{HyperPCM} reaches $0.5983 \pm 0.0368 / 0.8155 \pm 0.0204$, and the paired bootstrap interval for \texttt{HyperPCM - base} crosses zero (Appendix Tables~\ref{tab:robustness-probes} and \ref{tab:bootstrap-summary}). The synthetic panel therefore remains useful, but its drug-cold ordering is sensitive to how chemically strict the split actually is.

\subsection{Real temporal shift favors a different strong baseline}

We next evaluate the same external panel on \texttt{BindingDB\_patent / patent\_temporal}, our primary real-OOD benchmark. Over three seeds, \texttt{DTI-LM} is the strongest model by mean \AUPRC{}, reaching $0.7825 \pm 0.0078$, compared with $0.7772 \pm 0.0086$ for \texttt{base} and $0.7753 \pm 0.0050$ for \texttt{HyperPCM}. Figure~\ref{fig:delta-summary} compresses the main benchmark story into within-benchmark \AUPRC{} deltas relative to \texttt{base}: synthetic cold splits favor different systems, while the promoted temporal/provenance benchmark favors \texttt{DTI-LM}. This advantage is also supported by paired bootstrap: \texttt{DTI-LM - base} $= +0.0054$ with 95\% CI $[+0.0028, +0.0079]$. At the same time, the absolute margin is small relative to the models' own seed variation, so we interpret it as a ranking reversal within the studied panel rather than as a large deployment-relevant gap by itself. A second temporal robustness probe strengthens this reading. On the non-patent benchmark built directly from the official BindingDB release, \texttt{DTI-LM} still beats \texttt{base} over matched 3-seed evaluation, reaching $0.6531 \pm 0.0066 / 0.7644 \pm 0.0041$ versus $0.6369 \pm 0.0173 / 0.7333 \pm 0.0081$ for \texttt{base}$\,$(Appendix Table~\ref{tab:robustness-probes}). This does not make the patent benchmark a pure time-only shift, but it does reduce the concern that the main winner change is driven only by patent provenance.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/benchmark_delta_summary.pdf}
  \caption{Mean within-benchmark \AUPRC{} delta relative to \texttt{base} under the external replacement panel. Error bars show paired bootstrap 95\% intervals where available. Because prevalence differs across benchmarks, the vertical scale should be read as a row-wise within-benchmark contrast rather than as a cross-benchmark effect-size scale.}
  \label{fig:delta-summary}
\end{figure}

This is the central winner change in the revised paper. The synthetic panel does not produce a single best model, but the promoted temporal/provenance benchmark favors \texttt{DTI-LM} by mean \AUPRC{}. The alternative-cutoff benchmark tells the same story over matched three-seed evaluation: on \texttt{patent\_temporal\_v2017}, \texttt{DTI-LM} reaches $0.7134 \pm 0.0027 / 0.6754 \pm 0.0039$, versus $0.7044 \pm 0.0058 / 0.6624 \pm 0.0079$ for \texttt{base}. The broader lesson is therefore not that one external baseline wins everywhere, but that both benchmark axis and model family change the apparent ranking within the studied panel.

\subsection{Temporal shift is structured across year bands and overlap levels}

To help explain why the temporal/provenance benchmark changes the ranking, we analyze the patent results by year band and by overlap bucket. Table~\ref{tab:patent-diagnosis-compact} makes the primary \AUPRC{} slices explicit, while Figure~\ref{fig:patent-diagnosis} visualizes the same structure. In the large 2019 slice, \texttt{DTI-LM} is strongest by mean \AUPRC{} ($0.7734 \pm 0.0071$), with \texttt{base} ($0.7626 \pm 0.0098$) and \texttt{HyperPCM} ($0.7637 \pm 0.0049$) close behind. To avoid over-reading the tiny 2021 cell, we pool 2020 and 2021 into a single recent stratum. In that pooled recent slice, the ordering flips: \texttt{base} reaches $0.8716 \pm 0.0121 / 0.8134 \pm 0.0133$, while \texttt{DTI-LM} and \texttt{HyperPCM} drop to $0.8533 \pm 0.0100 / 0.7938 \pm 0.0166$ and $0.8516 \pm 0.0139 / 0.7925 \pm 0.0209$, respectively.

The overlap-bucket analysis reveals another useful pattern. In the lowest-overlap bucket, \texttt{DTI-LM} is strongest by mean \AUPRC{} ($0.6353$), while \texttt{HyperPCM} is close behind. In the middle-overlap bucket, \texttt{HyperPCM} takes a small lead on \AUPRC{} ($0.7513$) over \texttt{DTI-LM} ($0.7489$) and \texttt{base} ($0.7218$). In the highest-overlap bucket, the ordering flips again and \texttt{base} dominates on both \AUPRC{} and \AUROC{}. Together, these results are consistent with the view that temporal OOD is not just a colder version of synthetic cold-start. Instead, it changes which subpopulations dominate the aggregate result, and therefore changes the model ranking.

\input{tables/table_patent_diagnosis_compact}

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/patent_shift_diagnosis.pdf}
  \caption{Patent temporal/provenance diagnosis in terms of mean \AUPRC{} under the external replacement panel. Left: pooled year-band slices, with 2021 merged into the recent stratum. Right: test-set overlap buckets computed from the saved overlap score. Error bars denote seed std over the matched 3-seed runs. The aggregate \texttt{DTI-LM} advantage is concentrated in the 2019 and lower-overlap strata, while \texttt{base} dominates the recent and highest-overlap slices.}
  \label{fig:patent-diagnosis}
\end{figure}

\subsection{Supporting target-cold OOD benchmarks still favor the plain base model}

We treat \texttt{BindingDB\_Ki / unseen\_target} and \texttt{DAVIS / unseen\_target} as supporting real-OOD benchmarks rather than primary headline axes. Under matched three-seed evaluation, neither promoted external baseline overturns the support-panel story. On \texttt{BindingDB\_Ki}, \texttt{base} reaches $0.6934 \pm 0.0257 / 0.7192 \pm 0.0068$, compared with $0.6648 \pm 0.0177 / 0.6936 \pm 0.0044$ for \texttt{DTI-LM} and $0.6515 \pm 0.0301 / 0.6757 \pm 0.0211$ for \texttt{HyperPCM}. On \texttt{DAVIS}, \texttt{base} reaches $0.4097 \pm 0.0275 / 0.8613 \pm 0.0153$, compared with $0.4090 \pm 0.0570 / 0.8718 \pm 0.0152$ for \texttt{DTI-LM} and $0.3840 \pm 0.0269 / 0.8610 \pm 0.0245$ for \texttt{HyperPCM}.

The \texttt{DAVIS} result is the closest supporting-panel case: \texttt{DTI-LM} is effectively tied with \texttt{base} on mean \AUPRC{} and slightly higher on mean \AUROC{}, but it still does not create a clean winner change of the kind seen on the promoted patent benchmark. \texttt{BindingDB\_Ki} is more decisive, with \texttt{base} ahead of both external baselines on both metrics. These auxiliary results are therefore consistent with the main claim of the paper, but they do not carry the same argumentative weight as the patent benchmark because they do not add a cleaner ranking-reversal axis than temporal/provenance shift.
